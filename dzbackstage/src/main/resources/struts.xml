<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE struts PUBLIC
        "-//Apache Software Foundation//DTD Struts Configuration 2.3//EN"
        "http://struts.apache.org/dtds/struts-2.3.dtd">

<struts>
        <constant name="struts.devMode" value="true"/>
        <!-- Global message resource; Otherwise you will have seperate message resource for each Action -->
        <constant name="struts.custom.i18n.resources" value="messages" />
        <!--
            This is important if you are planning to have slashes in Action URLs
            e.g. In this demo, employee is deleted using URL /delete/10
            This this is set to false; then struts will try to find mapping for
            URL "/10" instaed of "/delete/10"
         -->
        <!--<constant name="struts.enable.SlashesInActionNames" value="true"/>-->
        <package name="dzbackstage" extends="struts-default">
                <!-- 指定action拦截器 -->
                <interceptors>
                    <interceptor name="authentication" class="com.dzbackstage.interceptor.BKAuthenticationInterceptor"/>
                </interceptors>

                <!-- 定义全局的result -->
                <global-results>
                    <result name="login" type="velocity">/login.vm</result>
                </global-results>

                <!-- class="sLogin" sLogin为spring的bean对应的name属性值 type="velocity" 添加来支持velovity模板 -->
                <action name="login" class="sLogin" method="execute">
                    <!--<result name="success" type="velocity">/index.vm</result>-->
                    <result name="success">/public/index.html</result>
                </action>

                <action name="index" class="sIndex" method="execute">
                    <!-- 使用拦截器 -->
                    <interceptor-ref name="authentication"/>
                    <result name="success">/public/index.html</result>
                    <!--<result name="success" type="velocity">/index.vm</result>-->
                </action>

        </package>
</struts>